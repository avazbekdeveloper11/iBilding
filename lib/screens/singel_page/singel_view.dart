import 'package:flutter/material.dart';

class SingelView extends StatelessWidget {
  const SingelView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
